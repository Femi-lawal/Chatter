<%= form_with model: current_account.active_account_relationships.find_by(followed: @account), local: true, html: { method: :delete } do |form| %>
  <%= form.submit "Unfollow", class: "btn btn-large btn-default" %>
<% end %>
