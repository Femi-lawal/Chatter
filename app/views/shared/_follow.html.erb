<%= form_with model: current_account.active_account_relationships.new(followed: account), local: true do |form| %>
  <%= form.hidden_field :followed_id %>
  <%= form.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>
